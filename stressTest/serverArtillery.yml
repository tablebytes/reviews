config:
  target: 'localhost:3001'
  phases:
    - duration: 60
      arrivalRate: 20
  processor:  "./randomRestaurant.js"
scenarios:
  - flow:
    - function: randomRestaurant
    - get:
        url: "/restaurants/{{random}}"
